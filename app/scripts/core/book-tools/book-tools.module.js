/*
    This factory is here to simulate a REST app
    not in book.service because here it's hard coded functions that should be replaced by REST app calls
 */
angular.module('core.bookTools', ['ngResource']);
